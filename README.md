# Claw Agents v1
Autonomous PRD-to-Code pipeline built on GitHub Actions.

## How it works
1. Edit `PRD.md` with software requirements.
2. The GitHub Action automatically triggers.
3. The AI agent processes the PRD and writes the architecture and code into `GENERATED_ARCHITECTURE.md`.
